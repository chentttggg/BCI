# Constitution for Cross-Agent EEG Raw Data Preprocessing

> 版本：0.1.0  
> 状态：活文档  
> 效力：最高纲领。所有参与 EEG 原始数据预处理的 agent 必须遵守本宪法。  
> 具体项目参数写入子文件（`mission/`、`tech_stack/`、`roadmap/`），子文件不得与本宪法冲突。

---

## 0. 定义与效力

- **原始数据**：从采集设备直接获得、未经任何信号处理或格式转换的文件。
- **派生数据**：由原始数据经过任何处理步骤生成的数据文件。
- **Agent**：参与预处理流程的自动化程序、模型或人类操作者。
- **质量门禁**：数据在进入下一阶段前必须满足的自动化或人工验收条件。
- **Provenance**：描述数据来源、处理步骤、参数、软件版本、输入输出哈希和操作者的元数据。

**冲突规则**：本宪法优先于任何子文件；子文件可以细化本宪法，但不得放宽或违反本宪法中的红线与原则。

---

## 1. Mission

### 1.1 总体使命

将 EEG 原始数据转化为**可信、可复现、可追溯、适合下游分析**的预处理数据，同时最大化信号保真度、科学完整性与操作透明度。

### 1.2 不可妥协原则

1. **原始数据只读**  
   任何 agent 不得修改、覆盖或删除原始 EEG 文件。所有处理必须从派生副本或只读访问开始。

2. **可复现性优先**  
   每一步处理必须记录软件版本、参数、随机种子、输入输出哈希和代码版本，确保结果可重放。

3. **可追溯性**  
   所有中间文件和最终文件必须携带 provenance 元数据，说明“谁、何时、如何、为何”生成该文件。

4. **保守预处理**  
   默认采用最小充分处理策略。当不确定某段数据是否应删除或修正时，应标记并保留，而不是静默删除。

5. **质量门禁不可跳过**  
   任何处理步骤的输出必须通过预设质量门禁后，才能进入下一阶段。QC 失败的数据必须被标记、报告或人工审核。

6. **可回滚性**  
   关键处理节点必须保存快照或中间产物，使流程能够回退到任意已通过 QC 的节点。

7. **并发与冲突控制**  
   同一文件同一时间只允许一个 agent 写入。写入必须采用原子操作，并附带版本号或哈希。禁止绕过接口直接修改他人产物。

8. **操作可解释性记录**
    不为了拟合结果而过度调参，一切调试朝向算法本身是不是最符合科学理论。对于重大技术调整记录到Tech_Stack文档中。

### 1.3 重大错误红线

以下行为被视为不可接受的重大错误，任何 agent 不得执行：

- 修改、覆盖、删除原始数据或未受版本控制的原始副本。
- 静默删除或插值通道、数据段，而不记录剔除/插值原因、比例和位置。
- 在未记录原始参考电极、通道位置、单位的情况下进行重参考或空间操作。
- 使用有损格式（如 CSV）或未定义单位的数据作为连续 EEG 的中间交换格式。
- 未通过质量门禁即向下游传递数据。
- 在未记录软件版本、参数、随机种子和输入输出哈希的情况下声称结果可复现。
- 在重采样、滤波或时间校正后未重新对齐或验证事件时间精度。
- 未授权访问、复制或共享原始 EEG 数据。

---

## 2. Tech Stack

### 2.1 核心技术栈

- **主语言**：Python 3.10+。MATLAB 可用于兼容性步骤，但必须封装为独立模块并记录运行环境。
- **核心库**：  
  - MNE-Python 为默认 EEG 处理库。  
  - NumPy、SciPy、pandas 用于通用数据处理。  
  - EEGLAB / FieldTrip 可作为兼容性补充，但输出必须转换回标准容器。
- **可视化与报告**：优先使用 MNE 内置绘图、matplotlib、seaborn 生成 QC 图。

### 2.2 数据与元数据标准

- **目录结构**：优先遵循 BIDS-EEG。
- **输入格式**：支持 EDF/BDF、BrainVision、FIF、set/fdt；导入后必须转换为统一中间格式。
- **中间格式**：优先使用 FIF（MNE）或 BIDS derivatives。连续 EEG 数据禁止使用 CSV 等有损格式。
- **元数据**：所有数据文件必须伴随 JSON sidecar，记录通道信息、参考电极、事件、滤波、坏导/坏段、处理历史等。
- **命名约定**：输出文件名必须包含参与者 ID、会话 ID、处理步骤、版本号，避免覆盖历史文件。

### 2.3 环境、版本与工作流

- **环境隔离**：使用 Docker/Singularity 容器或 conda/mamba 锁定环境。依赖必须锁定版本，并提供 `environment.yml` 或 `requirements.lock`。
- **代码版本控制**：Git 管理所有代码、配置和文档。数据处理代码必须关联 commit hash。
- **数据版本控制**：使用 DVC 或 git-annex 管理大文件，记录数据哈希和版本。
- **工作流**：流程必须 DAG 化，推荐 Snakemake / Nextflow 或等效可重放脚本。必须支持断点续跑、结构化日志和资源管理。
- **日志**：关键步骤输出结构化日志（如 JSON Lines），至少包含时间戳、agent ID、输入输出文件、参数和状态。

### 2.4 质量评估与报告

- **自动化 QC 指标**：包括但不限于通道幅度、频谱、线噪声、坏段比例、坏导数量、伪迹残留、事件一致性。
- **报告生成**：每个参与者/会话生成标准化 QC 报告（HTML/PDF），并归档。
- **门槛原则**：QC 指标必须在子文件中定义阈值；未达标数据不能进入下一阶段。

---

## 3. Roadmap (活文档)

### 3.1 标准预处理阶段

1. **Stage 0 — 数据接收与完整性校验**  
   - 校验文件数量、格式、哈希、大小。  
   - 建立参与者/会话清单，记录原始数据状态。  
   - 退出标准：所有文件通过完整性校验并入库。

2. **Stage 1 — 导入与标准化**  
   - 转换为统一中间格式；提取元数据；统一通道名、类型、单位；解析并验证事件。  
   - 退出标准：BIDS 或项目标准验证通过，元数据完整。

3. **Stage 2 — 初始质量检查与标注**  
   - 检测坏导、坏段、漂移、线噪声、异常事件。  
   - 生成初始 QC 报告；标记问题，不直接删除。  
   - 退出标准：QC 报告完成，问题清单生成。

4. **Stage 3 — 基础信号处理**  
   - 去趋势、重参考、滤波、降采样等。  
   - 每一步必须记录参数和原因。  
   - 退出标准：基础处理完成，频域/时域检查通过。

5. **Stage 4 — 伪迹处理**  
   - 剔除/插值坏导，标记坏段；使用 ICA / SSP / 回归等方法去除伪迹。  
   - 保留伪迹成分、剔除前后快照和参数。  
   - 退出标准：伪迹残留低于项目阈值，数据可追溯。

6. **Stage 5 — 最终质量门禁与发布**  
   - 最终 QC、数据封装、生成 BIDS derivatives、编写数据字典和变更日志。  
   - 退出标准：通过项目级验收标准，可被下游分析安全使用。

### 3.2 里程碑

- **M0**：原始数据入库，完整性校验通过。
- **M1**：标准化转换完成，初始 QC 报告发布。
- **M2**：基础信号处理与伪迹清理完成，中间数据可回放。
- **M3**：最终数据集发布，附带完整 provenance 和 QC 报告。

### 3.3 活文档维护规则

- 任何流程、工具或阶段的变更必须通过 pull request 更新本文件或相应子文件。
- 版本号遵循语义化版本：主版本（流程重构）、次版本（阶段调整）、补丁（文档修正）。
- 每次修订必须记录日期、变更者、变更理由和影响范围。
- 至少在每个项目启动前和结束后回顾本路线图与实际流程的一致性。
- 子文件可扩展具体参数、电极布局、任务事件、被试标准等内容，但不得违反本宪法原则。

---

**本宪法自发布起生效，所有 agent 必须遵守。**